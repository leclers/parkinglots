
<div id="booking-page">
  <div class="container">
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1 col-md-8 col-md-offset-2">

        <div id="booking-info">
          <% if @booking.user == current_user %>
          <!-- render the page for the user -->
            <%= render "booking-user" %>

          <% elsif @booking.parking.user == current_user %>
          <!-- render the page for a parking owner -->
            <%= render "booking-owner", {booking: @booking} %>

          <% end %>

        </div>

        <%= link_to 'back to your bookings', bookings_path, class: 'btn btn-info btn-block' %>


      </div>
    </div>
  </div>

</div>



